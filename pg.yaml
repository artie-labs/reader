source: postgresql

kafka:
  topicPrefix: abc
  #topicPrefix: foo
  bootstrapServers: localhost:29092
  publishSize: 5

postgresql:
  host: 0.0.0.0
  port: 5432
  userName: postgres
  password: postgres
  database: postgres
  disableSSL: true
  tables:
    - name: customers
      schema: inventory
      limit: 4
